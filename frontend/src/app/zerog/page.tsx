import ZeroGUploader from '@/components/UploadTo0g';

export default function ZeroGPage() {
    return (
        <div className="min-h-screen bg-gradient-to-br from-blue-50 to-purple-50">
            <ZeroGUploader />
        </div>
    );
}
